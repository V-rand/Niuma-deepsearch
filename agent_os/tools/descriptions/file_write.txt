创建或覆盖文件。文件已存在时会被覆盖，不存在时创建。

参数：
- path: 文件路径（相对工作目录）
- content: 要写入的内容
- encoding: 文件编码，默认 utf-8

返回：
- path: 写入的文件路径
- size: 写入的字符数

注意：
- 写入 .md 文件后会自动同步到 workspace_search 索引
- uploads/ 目录为只读区，写入会报错